#include <stdio.h>

    int main(){
        int x,y;
        printf("The value of x is \n");
        scanf("%d", &x);

        printf("The value of y is \n");
        scanf("%d", &y);

        int r,$;
        r=sqrt(x*x+y*y);
        $=tan
    
    return 0;
}