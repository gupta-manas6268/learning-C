// Q.15: When interest compounds q times per year at an annual rate of
//        r % for n years, the principal p compounds to an amount a as per
//        the following formula
//        a = p ( 1 + r / q ) nq
//        Write a program to read 10 sets of p, r, n & q and calculate the
//        corresponding as.






















#include <stdio.h>

int main(){
    int q, r, n, p, a;
    int a = p * pow((1 + r/q), n*q);t

    
    return 0;
}