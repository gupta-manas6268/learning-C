#include <stdio.h>

    int main(){
        // These operators "+=, -=, *=, /=, %=" are compound assignment operators. They modify the value of the operand to the left of them. 
        printf("Hello ");
        printf("world\n");
        int a = 1;
        // Loops are used to repeat similar part of a code snippet efficiently
        // (
        // printf("%d\n", a);
        // a++;) =---→ 100 times
    return 0;
    }
        