#include <stdio.h>

int main(){
    // Ans -→ structure.
    // As here we have to store similar data type.
    // So array is more useful to store similar data type.
    // And structure is more useful to store dissimilar data type.
    return 0;
}