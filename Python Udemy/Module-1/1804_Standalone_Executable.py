# Firstly Terminal Setup by 'set -executionpolicy remotesigned -scope currentuser'
#  & then 'pip install pyinstaller'.

# I currently have Terminal setup.

# Then, type 'pyinstaller --onefile --windowed --clean filename.py' in Terminal.
# Ex: 'pyinstaller --onefile --windowed --clean 1801_todos.py'

# It will make a 'build' & 'dist' folder and also make 'filename.spec' file.

#  Executable file is in 'dist' folder. 
# You can run only '1801_todos.exe' by double-click & only have to share '1801_todos.exe'
#  to other people's.