// It is from book.
#include <stdio.h>

    int main(){
        int p,n;
        float si, r;
        si=p*n*r/100;

        printf("Enter values of p,n,r\n");
        scanf("%f %f %f", &p, &n, &r);

        printf("%f\n",si);
        
    
    return 0;
}