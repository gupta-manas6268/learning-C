// This is Lecture-06.

// In Slide-06,

// In Page-07,
//  1, 2, .... k, (N => Total coins, k => Max. coins we can pick at each turn.)
//  if(N % (k+1) == 0){ 2nd player wins}
//  else{ 1st player wins}

// In Page-13,
//  N = 2 * (cnt fo odd divisors)      // (cnt => count)

// In Page-17,
//  if((c[1] ^ c[2] ^ c[3] ..... ^ c[n]) == 0){
//      Player currently making the move loose the game.
//  }


// In Page-24,
//  GN(0) = 0, ....  (GN => Grundy Number)

// In Page-26,
//  GN(N) == 0, => (I will lose),    (GN => Grundy Number)

// In Page-28,
//  (mex => Minimum Excluded)


// In Page-33,
//  GN(N[1]) ^ GN(N[2]) ^ GN(N[3]) ^ GN(N[4]) != 0