// In Slide-1

// In Page-3
//  (rem_b => remaining_budget.)

// In Page-4
//  (B => Budget.)

// In Page-14
//  (Columns => O(n).)

// In Page-16
//  = No. of states * Avg T.T    (T.T = Transition Time Per State.)
//  = N * O(N/2) = O(power(N, 2))

// In Page-23
//  m.erase(x) -> all instances of X
//  m.erase(m.find(x)) -> delete only 1-X

// In Page-25, we have totally different Question, then in 
//     Page-24, where problem is unsolvable because of use of 'Segment Trees'
//      which is from TLE-4, Unit-9.








// In Slide-2

// In Page-8
//  j -> no. of 3's we have placed yet.
//                                 (↑)

// In Page-9
//  -> max. score we can get from index (i .... n-1)
//                   (↑)

// In Page-14
//  2 - 3's

// In Page-16
//  (Always remove the largest state)

// In Page-17
//  Cyclic DP states
//    (↑)

// In Page-18
//  Expected no. of coin tosses you need to get 1-head.
//     (↑)

// In Page-20
//  E(x) - (1/2)*(E(x)) = 1/2 + 1/2 = 1
//      (E(x) => Expected no. of moves to get 1-head.)