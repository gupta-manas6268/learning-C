#include<bits/stdc++.h>
using namespace std;

#define endl '\n'
#define int long long 

const int MOD = 1e9 + 7;
const int INF = LLONG_MAX >> 1;

signed main(){
    ios::sync_with_stdio(false); cin.tie(NULL);

    // Time complexity is approximate amount of operations
    // in a code.
    // It is a function, Ex-→f(x) = n + n^2 + log(n) + 5.
    // Here, in this code significant digit is (n^2). Because it grows at a very high speed compared to other variables in time complexity.

    //   Here, n^2 = pow(n, 2).

    // (10^8)operations = 1second of run code.


    // '+' and '%' are both arithmetic operators ,
    // but, time taken by '%' is higher than time taken by '+'. 


}