#include<bits/stdc++.h>
using namespace std;

#define endl '\n'
#define int long long 

const int MOD = 1e9 + 7;
const int INF = LLONG_MAX >> 1;

// int func(int n=1) {
//     cout << n << endl; // As, we are not using return here. So, we do not use 'int' instead we use 'void'. 
//                        //   Because by using 'int' the code can crash and then the code will take much more time to run.
// }
void func(int n=1) {
    cout << n << endl;
}

signed main(){
    ios::sync_with_stdio(false); cin.tie(NULL);

    func();
}