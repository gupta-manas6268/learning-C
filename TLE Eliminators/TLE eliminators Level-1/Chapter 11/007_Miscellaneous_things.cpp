#include<bits/stdc++.h>
using namespace std;

#define endl '\n'
#define int long long 

const int MOD = 1e9 + 7;
const int INF = LLONG_MAX >> 1;

int func(int n=1) {
    if(n == 1){
        return 0;
    }
    if(n != 1){
        return 1; // This code doesn't give any error, but this code will not work when run. 
                  //  Because C++ allow this to do. So, just remember it.
    }
}

signed main(){
    ios::sync_with_stdio(false); cin.tie(NULL);

    func();    
}