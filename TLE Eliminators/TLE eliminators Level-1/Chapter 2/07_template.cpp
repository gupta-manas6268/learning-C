#include<bits/stdc++.h>
using namespace std;

#define endl '\n' // To convert 'endl' to '\n' .So, we have no error because of fast I/O.
#define int long long // Convert 'int' to 'long long int' for competitive programming. 

const int MOD = 1e9 + 7; // To convert big values to scientific notation.
const int INF = LLONG_MAX >> 1; // just / 2

signed main(){
    ios::sync_with_stdio(false); cin.tie(NULL); // fast I/O

    int tc; cin >> tc; // tc -→ number of test cases.

    while (tc--){ // loop for all test cases.

    }
}