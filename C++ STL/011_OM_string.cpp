#include<bits/stdc++.h>
using namespace std;

int main(){
    map <string, string> m;
    m["abc"] = "abcd";  // TC of Insertion = s.size() * log(n).
    // here, s.size() -> size of string in "abc" in 'm["abc"]'.

    return 0;
}