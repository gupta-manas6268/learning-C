t

// Q.1: Create a function (Hint: Make it a friend function) which takes 2 point object and computes
//       the distance between those 2 points.


#include<bits/stdc++.h>
using namespace std;

class points{
    int x1, y1, x2, y2;
    friend points distance(points x1, points y1, points x2, points y2);
public:

};

points distance(points)

int main(){
    int x1, y1, x2, y2;
    cin >> x1 >> y1 >> x2 >> y2;


    return 0;
}