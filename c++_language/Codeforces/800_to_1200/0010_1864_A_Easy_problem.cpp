// Q.10: https://codeforces.com/problemset/problem/1864/A








#include<bits/stdc++.h>
using namespace std;

#define endl '\n'
#define int long long 

const int MOD = 1e9 + 7;
const int INF = LLONG_MAX >> 1;

signed main(){
    ios::sync_with_stdio(false); cin.tie(NULL);

    int tc; cin >> tc;

    while (tc--){
        int x, y, n; cin >> x >> y >> n;
        int a;

        if((y-x) >= ((n * (n-1))/2)){
            a = ((y-x)/ ((n * (n-1))/2));

            
        }
    }
}