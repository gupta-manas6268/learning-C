#include<bits/stdc++.h>
using namespace std;

#define endl '\n'
#define int long long 

const int MOD = 1e9 + 7;
const int INF = LLONG_MAX >> 1;

signed main(){
    ios::sync_with_stdio(false); cin.tie(NULL);

    char s[100]; cin>>s;
    int a = strlen(s);
    int element;
    
    for(int i=0; i<=(a+1)/2; i++){
        vector<int>vec1((a+1)/2);

    }
}