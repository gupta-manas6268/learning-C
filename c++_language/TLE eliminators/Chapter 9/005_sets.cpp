#include<bits/stdc++.h>
using namespace std;

#define endl '\n'
#define int long long 

const int MOD = 1e9 + 7;
const int INF = LLONG_MAX >> 1;

signed main(){
    ios::sync_with_stdio(false); cin.tie(NULL);

    // unordered_set<int> s = {1, 2, 4}, t = {5, 4, 2};
    set<unordered_set<int>> s;

    unordered_set<int> a, b;
    s.insert(a);
    
}