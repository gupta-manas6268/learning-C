#include<bits/stdc++.h>
using namespace std;

#define endl '\n'
#define int long long 

const int MOD = 1e9 + 7;
const int INF = LLONG_MAX >> 1;

signed main(){
    ios::sync_with_stdio(false); cin.tie(NULL);

    // It is a not a example of elementary operation.
    // Because string is not a elementary operation,
    // because size of string is not fixed.
    string s, t;
    cin>>s>>t;

    // if(s < t){
    //     cout<<"s is less than t"<<endl;
    // }

    if(s == t){
        cout<<"s is equal to t"<<endl;
    }

}