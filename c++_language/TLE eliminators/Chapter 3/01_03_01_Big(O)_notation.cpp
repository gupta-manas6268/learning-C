#include<bits/stdc++.h>
using namespace std;

#define endl '\n'
#define int long long 

const int MOD = 1e9 + 7;
const int INF = LLONG_MAX >> 1;

signed main(){
    ios::sync_with_stdio(false); cin.tie(NULL);

    // If in Question, there is a if-else statements.
    // where if(then TC = n^2), else(then TC=nlog(n)). // TC = Time Complexity

    // Then, Big(O) = n^2 (because n^2 is worst case scenario.)

    // If, worst case time complexity is O(n^2).  // It is very important time complexity, among these three types of time complexity.
    // then, average time complexity is theta(n*log(n)).
    // then, best case time complexity is omega(n).

    
    // Example function: 2(N^2) + 4*N + 4*((M^3) + 5) + 10.
    // Ans -→ N^2 + M^3.

}