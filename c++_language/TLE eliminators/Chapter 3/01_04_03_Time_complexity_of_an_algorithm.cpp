#include<bits/stdc++.h>
using namespace std;

#define endl '\n'
#define int long long 

const int MOD = 1e9 + 7;
const int INF = LLONG_MAX >> 1;

signed main(){
    ios::sync_with_stdio(false); cin.tie(NULL);

    // for(int i=1; i<n; i *= 2){

    // }

    // i = (2^k) = n
    // k = log(n) with base '2'.
    

    
    // Here, O(log(n) with base '2'.)
}