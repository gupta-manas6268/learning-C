// Q.1: void recurse(int n) {
//          if (n == 0) return;
//          recurse(n-1);
//      }

// Ans: TC = O(n)


// Q.2: void recurse(int n) {
//          if (n == 0) return;
//          recurse(n/2);
//      }

// Ans: TC = O(log(n) with base 2)

// Q.3: void recurse(int n) {
//          if (n == 0) return;
//          recurse(n-1);
//          recurse(n-1);
//      }

// Ans: TC = O(pow(2, n))               // I explain this Question in Handwritten Notes properly.

// Q.4: void recurse(int n) {
//          if (n == 0) return;
//          recurse(n/2);
//          recurse(n/2);
//      }

// Ans: TC = O(n)                       // I explain this Question in Handwritten Notes properly.