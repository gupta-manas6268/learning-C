#include<bits/stdc++.h>
using namespace std;

int main(){
    // ios::sync_with_stdio(false); cin.tie(NULL); // In debugging by commenting this line we are saving lots 
                                                   // because these 'fast IO' takes lots of time.    
    int tc; cin>>tc;

    while(tc--){
        int n; cin>>n;
        cout<<n + 2 <<endl;
    }

    return 0;
}