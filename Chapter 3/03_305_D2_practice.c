#include <stdio.h>

    int main(){
        float a,b,c;
        printf("Enter the three sides of the triangle");
        scanf("%f %f %f",&a,&b,&c);

        if(!{
            printf("It is not a valid triangle \n");
        }
    
    return 0;
}