#include<bits/stdc++.h>
using namespace std;

int main(){
    // #ifndef ONLINE_JUDGE
    // freopen("input.txt", "r", stdin);
    // freopen("output.txt", "w", stdout);
    // #endif

    // int x = 10, y = 5;

    // cout << !(x>=y);
    // cout << (!x>=y);
    // cout << (!!x>=y);

    // cout << (++x) << endl;
    // cout << (x++) << endl;

    int x=24, y=34, z=67;
    if(x>=y && x>=z){
        cout << "x is greatest";
    }
    else if(y>=x && y>=z){
        cout << "y is greatest";
    }
    else{
        cout << "z is greatest";
    
    }
    if()

    return 0;
}